import logo from '../assets/images/logo.png'

const Header = () => {
  return (
    <div>
        <div>
            <img src={logo} alt="my logo" className='' />
        </div>
    </div>
  )
}

export default Header